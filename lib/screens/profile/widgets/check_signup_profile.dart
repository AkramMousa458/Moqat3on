import 'package:flutter/material.dart';

class CheckSignUpProfile extends StatelessWidget {
  const CheckSignUpProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Moqata3a|مقاطعة'),
      ),
      body: Container(),
    );
  }
}
